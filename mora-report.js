async function loadMoraReport(){const host=$('moraReportBody');if(!host)return;const{data,error}=await sb.from('documentos_cartera').select('id,cartera_id,cliente_id,vendedor_actual_id,vencimiento,saldo,estado,cliente:cartera_clientes(nombre,codigo),vendedor:cartera_vendedores(nombre),cartera:carteras(nombre,moneda)').eq('estado','VIGENTE').lt('vencimiento',new Date().toISOString().slice(0,10)).order('vencimiento');if(error){console.error(error);toast(`No se pudo cargar mora: ${error.message}`);return}const rows=data||[];const groups=new Map();for(const x of rows){const key=`${x.cartera_id}|${x.cliente_id}`;let g=groups.get(key);if(!g){g={cartera:x.cartera?.nombre||'',moneda:x.cartera?.moneda||'USD',cliente:x.cliente?.nombre||'',codigo:x.cliente?.codigo||'',vendedor:x.vendedor?.nombre||'SIN VENDEDOR',saldo:0,masAntigua:x.vencimiento};groups.set(key,g)}g.saldo+=Number(x.saldo||0);if(x.vencimiento<g.masAntigua)g.masAntigua=x.vencimiento}const today=new Date();const out=[...groups.values()].map(g=>({...g,dias:Math.max(0,Math.floor((today-new Date(`${g.masAntigua}T00:00:00`))/86400000))})).sort((a,b)=>b.saldo-a.saldo);host.innerHTML=out.map(g=>`<tr><td>${esc(g.cartera)}</td><td>${esc(g.cliente)}</td><td>${esc(g.codigo)}</td><td>${esc(g.vendedor)}</td><td>${showDate(g.masAntigua)}</td><td>${g.dias}</td><td>${fmt(g.saldo,g.moneda)}</td></tr>`).join('');const total=out.reduce((a,g)=>a+g.saldo,0);$('moraTotal').textContent=out.length?`${out.length} clientes · ${fmt(total,out[0]?.moneda||'USD')}`:'0';}
window.loadMoraReport=loadMoraReport;window.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.tab[data-view="mora"]').forEach(b=>b.addEventListener('click',()=>setTimeout(loadMoraReport,0)));});