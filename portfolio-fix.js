window.addEventListener('load',async()=>{try{const{data,error}=await sb.from('carteras').select('*').order('orden');if(error)throw error;const opts='<option value="">Seleccione cartera destino</option>'+(data||[]).map(c=>`<option value="${c.id}">${esc(c.nombre)}</option>`).join('');const a=$('importPortfolio'),b=$('portfolioSelect');if(a)a.innerHTML=opts;if(b)b.innerHTML=(data||[]).map(c=>`<option value="${c.id}">${esc(c.nombre)}</option>`).join('')}catch(e){console.error('No se pudieron restaurar las carteras',e)}});